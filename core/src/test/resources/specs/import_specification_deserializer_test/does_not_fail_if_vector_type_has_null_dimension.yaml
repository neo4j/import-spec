version: "1"
sources:
  - type: bigquery
    name: my-bigquery-source
    query: SELECT id, embeddings FROM my.table
targets:
  nodes:
    - name: my-node
      source: my-bigquery-source
      labels: [ALabel]
      properties:
        - source_field: embeddings
          target_property: embeddings
          target_property_type:
            name: FLOAT
