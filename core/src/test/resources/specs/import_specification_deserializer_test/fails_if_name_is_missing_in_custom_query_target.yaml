---
version: '1'
sources:
- type: bigquery
  name: a-source
  query: SELECT id, name FROM my.table
targets:
  queries:
  - source: a-source
    query: UNWIND $rows AS row CREATE (n:ANode) SET n = row
