version: '1'
config:
  enable_graph_type: true
sources:
  - name: movies-source
    type: jdbc
    sql: SELECT id, title FROM db.movies
targets:
  nodes:
    - name: movies
      source: movies-source
      labels:
        - Movie
        - VisualArt
        - WorkOfArt
      properties:
        - source_field: id
          target_property: identifier
        - source_field: title
          target_property: title
          target_property_type: string
        - source_field: embeddings
          target_property: embeddings
          target_property_type:
            name: float_vector
            dimension: 42
      schema:
        key_constraints:
          - name: movies-identifier-key-constraint
            label: Movie
            properties:
              - identifier
        unique_constraints:
          - name: movies-title-unique-constraint
            label: Movie
            properties:
              - title
        type_constraints:
          - name: movies-title-type-constraint
            label: Movie
            property: title
          - name: movies-embeddings-type-constraint
            label: Movie
            property: embeddings
